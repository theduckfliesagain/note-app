const hostURL = "http://localhost:3000/posts";

async function getPost(){
    try {
        console.log(window.location.pathname);
        // const response = fetch(hostURL + )
    } catch (error) {
        
    }
}
